class MembersController < ApplicationController

  def index
  
  end

  def new
    # @item = Furima.new
  end

  def create
    # @item = Furima.new(item_params)
  end

  def deit

  end

  def destroy

  end

  def update

  end

  def show

  end

private

# def user_params
#   params.require(:user).permit(:image, :name, :kana_name, :email, :encrypted_password, :phone_numder, :area_id, :village, :address, :building_name, :birtday, :license_table, :license_back,).merge(user_id: current_user.id)

end
